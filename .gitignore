# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# OpenCode binary
opencode/bin/opencode

# Dependencies
node_modules/

# Handoffs (private per-machine, template is in repo)
opencode/handoffs/

# Personal overrides (use private repo instead)
AGENTS.md.local
rate-limit-fallback.json
config/agents/
