# Masha

## WiFi
- Сброс: `modprobe -r r8723bs && modprobe r8723bs` (alias: `wifi.ud`)

## Systemd
- Рестарт автозапуска: `sudo systemctl restart masha-runner`

## Python
- venv: `source /mnt/storage/venvs/tools/bin/activate`
